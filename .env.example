# Frontend env (rename to .env.local for Vite)
# mock = run entirely in the browser with localStorage (default, works in preview)
# real = call the Express + MySQL backend
VITE_API_MODE=mock
VITE_API_BASE_URL=http://localhost:5000/api
